// import {create} from 'zustand';

// // Define a type for the state
// interface CartState {
//   items;
 
// }

// // Create the Zustand store
// const useSingleProdCountInCart = create<CartState>((set) => ({
//   items: [],
//   add: (item: string) =>
//     set((state) => ({
//       items: [...state.items, item],
//     })),
// }));

// export default useCartStore;
